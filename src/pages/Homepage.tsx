import { SearchBar } from '../components/SearchBar/SearchBar';

const Homepage = () => {
  return (
    <div>
      <p>HomePage</p>
      <SearchBar placeholder="Search Home" />
    </div>
  );
};

export default Homepage;
